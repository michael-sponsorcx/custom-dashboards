/**
 * Stub for Stadium PageHeader filter compatibility.
 * getCategoryIcon is used in inline/slideout filter rendering for 'categories' filter key.
 */

import type { ReactNode } from 'react';

export const getCategoryIcon = (_label: string): ReactNode => {
  return null;
};
