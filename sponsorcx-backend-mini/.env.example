# Server Configuration
PORT=8080
NODE_ENV=development
# CORS Configuration - Currently maximally open for development
# Set to specific origins in production (e.g., https://yourdomain.com)
CORS_ORIGIN=*

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sponsorcx
DB_USER=sponsorcx_user
DB_PASSWORD=your_db_password
DB_SSL=false

# Optional: Connection pool settings
DB_POOL_MIN=2
DB_POOL_MAX=10

# Cube API Configuration
CUBE_API_BASE_URL=https://spotty-beetle.aws-us-east-2.cubecloudapp.dev/user/michael%40sponsorcx.com/1/cubejs-api
CUBE_API_TOKEN=your_cube_api_token_here

# Cache Configuration (in minutes)
CUBE_METADATA_CACHE_TTL=5
CUBE_SCHEMA_CACHE_TTL=30
