/**
 * Stub for Stadium PageHeader FilterSlideOutCustomFields compatibility.
 */

const useCustomFields = (_objectType: string) => {
  return {
    customFields: [],
    loading: false,
    error: null,
  };
};

export default useCustomFields;
