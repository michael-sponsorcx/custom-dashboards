{
  "buildCommand": "yarn build:frontend",
  "outputDirectory": "dist",
  "installCommand": "rm -f yarn.lock && yarn install --network-timeout 100000",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
